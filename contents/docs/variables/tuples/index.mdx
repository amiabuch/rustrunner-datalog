---
title: Tuples
description: We move ahead from variables to tuples
---

## Assignment
```rust
fn main() {
    let (x, y) = (1, 2);
}
```

## Destructuring
```rust
fn main() {
    let (mut x, y) = (1, 2);
    x += 2;

    assert_eq!(x, 3);
    assert_eq!(y, 2);

    println!("Success");
}
```


